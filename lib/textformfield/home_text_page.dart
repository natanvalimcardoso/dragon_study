import 'package:flutter/material.dart';

class HomeTextPage extends StatelessWidget {

  const HomeTextPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Texte Textfield'),),
           body: Container(),
       );
  }
}